import type { Route } from "./+types/RecipeResultEdit";

export function meta({}: Route.MetaArgs) {
  return [{ title: "Editar Resultado" }];
}

export default function RecipeResultEdit() {
  return null;
}
